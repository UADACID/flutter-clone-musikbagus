import 'package:flutter/material.dart';

class Musikologi extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Musikologi'),
    );
  }
}
