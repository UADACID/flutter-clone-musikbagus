import 'package:flutter/material.dart';

class Musikalisme extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Musikalisme'),
    );
  }
}
